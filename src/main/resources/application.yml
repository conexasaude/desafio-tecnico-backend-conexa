server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/schedulingappoinment
    username: root
    password: root
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
    baselineOnMigrate: true

api:
  security:
    token:
      secret: "seu_valor_do_token_aqui"
