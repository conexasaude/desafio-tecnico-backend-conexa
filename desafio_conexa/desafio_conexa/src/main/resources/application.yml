security:
    jwt:
        token:
            secret-key: 53cr37
            expire_length: 3600000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/conexa_saude?useTimezone=true&ServerTimezone=UTC
    username: root
    password: root   
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false
springdoc:
    pathsToMatch: 
      - /api/v1/**
    swagger-ui:
        use-root-path: true 
      
